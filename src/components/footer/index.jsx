import React from 'react';

const Footer = () => {
    
    return (
        <div>
            Footer
            <button onClick={()=> console.log('test')}>
              Watch now
            </button>
        </div>
    );
}

export default Footer;
